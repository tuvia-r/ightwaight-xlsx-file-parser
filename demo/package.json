{
  "name": "xlsx-file-parser-demo",
  "version": "1.0.2",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && webpack --mode=production && cp src/index.html dist",
    "serve": "webpack serve --mode=development"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "xlsx-file-parser-light": "0.0.7-0"
  },
  "devDependencies": {
    "html-loader": "^4.1.0",
    "node-polyfill-webpack-plugin": "^2.0.1",
    "webpack-cli": "^4.10.0",
    "webpack-dev-server": "^4.10.1"
  }
}
