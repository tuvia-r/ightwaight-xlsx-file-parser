{
  "name": "xlsx-file-parser-demo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && parcel build src/index.js && cp src/index.html dist",
    "serve": "rm -rf dist && parcel src/index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "xlsx-file-parser-light": "0.0.4"
  },
  "devDependencies": {
    "assert": "^2.0.0",
    "browserify-zlib": "^0.2.0",
    "buffer": "^6.0.3",
    "constants-browserify": "^1.0.0",
    "events": "^3.3.0",
    "parcel": "^2.7.0",
    "path-browserify": "^1.0.1",
    "process": "^0.11.10",
    "stream-browserify": "^3.0.0",
    "util": "^0.12.4"
  }
}
